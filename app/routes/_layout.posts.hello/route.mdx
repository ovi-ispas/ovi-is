---
title: Hello, World!
description: First post
slug: hello
---

export const meta = () => {
  return [
    { title: frontmatter.title },
    {
      name: 'description',
      content: frontmatter.description,
    },
  ]
}

# {frontmatter.title}
